name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  PACKER_VERSION: 1.15.4

jobs:
  setup:
    runs-on: ubuntu-24.04
    outputs:
      images: ${{ steps.gen.outputs.images }}
      timestamp: ${{ steps.gen.outputs.timestamp }}
      gitref: ${{ steps.gen.outputs.gitref }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - id: gen
        run: |
          echo "images=$(jq -c . images.json)" >> "$GITHUB_OUTPUT"
          echo "timestamp=$(date -u +%Y%m%d%H%M)" >> "$GITHUB_OUTPUT"
          echo "gitref=${GITHUB_SHA:0:7}" >> "$GITHUB_OUTPUT"

  build:
    needs: setup
    runs-on: ubuntu-24.04
    name: ${{ matrix.image.version }}-${{ matrix.image.arch }}-${{ matrix.flavor }}
    # packer init resolves plugins through the GitHub API; authenticate so the
    # parallel matrix does not exhaust the 60/hr unauthenticated limit that is
    # shared across all jobs on a runner's egress IP.
    env:
      PACKER_GITHUB_API_TOKEN: ${{ github.token }}
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJSON(needs.setup.outputs.images) }}
        flavor: [base, full]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      # GHA runners expose /dev/kvm but gate it behind the kvm group, which the
      # runner user is not a member of. This udev rule opens the device so qemu
      # can use -accel kvm without sudo. Standard recipe (actions/runner-images#8542).
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Install QEMU and Packer
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-system-arm qemu-efi-aarch64 qemu-utils xz-utils netcat-openbsd xorriso
          wget -q "https://releases.hashicorp.com/packer/${PACKER_VERSION}/packer_${PACKER_VERSION}_linux_amd64.zip"
          sudo unzip -o "packer_${PACKER_VERSION}_linux_amd64.zip" -d /usr/local/bin
          packer version

      - name: Cache OpenBSD ISO
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: iso
          key: openbsd-iso-${{ matrix.image.version }}-${{ matrix.image.arch }}-${{ matrix.image.iso_checksum }}

      - name: Build image
        run: |
          # No ACCEL: the Makefile picks kvm for the native amd64 build and tcg
          # for the cross-arch (emulated) arm64 build.
          make build \
            VER="${{ matrix.image.version }}" \
            ARCH="${{ matrix.image.arch }}" \
            FLAVOR="${{ matrix.flavor }}" \
            ISO_CHECKSUM="${{ matrix.image.iso_checksum }}"

      - name: Smoke test image
        run: |
          make smoke \
            VER="${{ matrix.image.version }}" \
            ARCH="${{ matrix.image.arch }}" \
            FLAVOR="${{ matrix.flavor }}"

      - name: Compress image
        if: ${{ success() && github.event_name == 'workflow_dispatch' }}
        run: |
          make compress \
            VER="${{ matrix.image.version }}" \
            ARCH="${{ matrix.image.arch }}" \
            FLAVOR="${{ matrix.flavor }}"

      - name: Upload artifacts
        if: ${{ success() && github.event_name == 'workflow_dispatch' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: openbsd-${{ matrix.image.version }}-${{ matrix.image.arch }}-${{ matrix.flavor }}
          path: output/build/${{ matrix.image.arch }}/${{ matrix.image.version }}/${{ matrix.flavor }}/*.img.xz
          if-no-files-found: error

  release:
    needs: [setup, build]
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      GH_TOKEN: ${{ github.token }}
      TS: ${{ needs.setup.outputs.timestamp }}
      GITREF: ${{ needs.setup.outputs.gitref }}
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          merge-multiple: true

      - name: Stamp assets
        run: |
          cd dist
          for f in *.img.xz; do
            mv "$f" "${f%.img.xz}-${GITREF}-${TS}.img.xz"
          done

      - name: Attest build provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        with:
          subject-path: dist/*.img.xz

      - name: Publish release
        run: |
          gh release create "$TS" dist/*.img.xz \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "OpenBSD images $TS" \
            --notes "OpenBSD cloud images built from commit \`$GITREF\`"
